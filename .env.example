# Local development only. Copy to .env and choose a local password.
DB_USERNAME=search_ranking
DB_PASSWORD=change-me-for-local-development
DB_URL=jdbc:postgresql://localhost:5432/search_ranking
DB_PORT=5432
SERVER_PORT=8080
ADMIN_TOKEN=
